
@{ var username = Session["username"]; }
@if (username == null)
{
<text>Welcome.  @Html.ActionLink("Log On", "LogOn", "Account")</text>
}
else
{
<text>Welcome, <b>@(username)</b>.  @Html.ActionLink("Log Off", "LogOff", "Account")</text> 
}